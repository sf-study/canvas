###HTMLCanvasElement.toDataURL()方法学习笔记

参考网站：

[点击打开](https://developer.mozilla.org/en-US/docs/Web/API/HTMLCanvasElement/toDataURL)

The HTMLCanvasElement.toDataURL() method returns a data URIs containing(包含，n) a representation（代表） of the image in the format（格式） specified（指定的） by the type parameter (defaults to PNG). The returned image is in a resolution（分辨率） of 96 dpi（ 每英寸点数）.

###Syntax：

canvas.toDataURL(type, encoderOptions);

**参数**